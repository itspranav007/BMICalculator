import React, { useState } from "react";
import logo from '../assets/APEXLOGO.jpg';

import { StyleSheet, Text, TextInput, TouchableOpacity, View, StatusBar, Button, Image, ImageBackground } from 'react-native';

export const Profile = () => {

    return (

        <View>
            <ImageBackground source={require = (logo)} style={{ width: '100%', height: '100%', }}>


            </ImageBackground>
        </View>

    )
}